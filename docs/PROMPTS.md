# Prompts

Useful instruction for future AI agents:

```text
Before changing code, read docs/AI_ONBOARDING.md, docs/MASTER_CONTEXT.md, docs/DECISIONS.md, and the files related to the task.

If code and docs conflict, treat code as the current implementation and update docs unless the user asks for code changes.

Do not invent product behavior. Keep changes focused. Run pnpm lint and pnpm build before committing.
```
